{
    "scripts": {
        "build": "dfx generate backend && cd canisters/frontend && npm install && npm run build",
        "typecheck": "tsc --noEmit",
        "test": "node test/test.js"
    },
    "dependencies": {
        "@dfinity/principal": "0.11.2",
        "azle": "0.4.0",
        "hash.js": "1.1.7",
        "js-sha256": "0.9.0"
    },
    "devDependencies": {
        "typescript": "4.4.4"
    }
}
